{
  "name": "blogging-app",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client/blog",
    "heroku-postbuild":"NPM_CONFIG_PRODUCTION=false npm install --prefix server  "
    
  }
}
